apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: collins6299/yolo-backend:1.0.8
          ports:
            - containerPort: 5000
          env:
            - name: MONGO_URI
              value: "mongodb+srv://ckiplimo54:Ngetich1234@yolo.nudp5jl.mongodb.net/darkroom-dev?retryWrites=true&w=majority"  
